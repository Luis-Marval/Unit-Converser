{
  "name": "convert-unit",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter ./back-end/ build",
    "lint": "eslint .",
    "preview": "vite preview"
  }
}
